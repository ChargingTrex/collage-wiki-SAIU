# macOS
.DS_Store

# Node / Docusaurus (once scaffolded in CP1)
node_modules/
build/
.docusaurus/
.cache-loader/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/

# Claude Code local state
.claude/
graphify-out/

# Playwright test artifacts (regenerated by `npm run test:e2e`)
/test-results/
/playwright-report/
/playwright/.cache/

# Superseded session snapshots and content drafts — kept locally for
# history, not published once this repo goes public. docs-internal/'s
# other files (animation-caveats.md, decap-cms-auth-todo.md) stay tracked;
# they're actively cited from CLAUDE.md/CONTRIBUTING.md.
/docs-internal/archive/

# Not project content — kept locally, never published. (These were
# mistakenly hard-deleted in an earlier cleanup commit instead of just
# untracked; restored to disk from git history and moved here instead.)
/prompt library.md

# Planning docs — working notes for in-progress/future features, not
# reference material for using or contributing to the repo. Kept locally.
/feature-images-recent-activity-plan.md
/github-pages-hosting-plan.md
/leadership-rollover-plan.md

# Local test run output, not reference material. Kept locally.
/TEST_REPORT.md
